- # in some cases the local variable 'team' is actually an organization... forgive me!

= form_for [team, invitation], html: { class: 'list-item-form' } do |f|
  %fieldset
    .field
      = f.label :full_name
      = f.text_field :full_name, placeholder: 'Benji Franklin'
    .field
      = f.label :email_address
      = f.email_field :email_address, placeholder: 'benji@idonethis.com'

  .actions= f.submit 'Invite', class: 'button utility'