.top-note Take 30 seconds to do a quick status update for your team.

.content
  .header
    .main-wrap
      %table
        %tr
          %td
            %h1 Your subscription is about to due
          %td
            %p.salutation #{ @organization.name.present? ? @organization.name : 'My Organization' }

  .main
    .main-wrap
      .important-text
        %p Hi #{ @owner.full_name.present? ? @owner.first_name : 'there' },

        %p Your membership of #{ @organization.name.present? ? @organization.name : 'My Organization' } is about to
          expire on <strong>#{Time.at(@upcoming_invoice[:created]).to_date}</strong>.

        %p We hope you’ve enjoyed benefits of your membership at <strong>I Done This</strong>.
  .footer
    %p.centered
      = link_to root_url do
        = image_tag "emails/we-love-progress.png", class: 'strapline', alt: 'We love progress'
