.top-note Take 30 seconds to do a quick status update for your team.

.content
  .header
    .main-wrap
      %table
        %tr
          %td
            %h1="#{@sender_user.full_name.present? ? @sender_user.first_name : 'there'} has assigned you a new task!"
          %td
            %p.salutation= @today.strftime('%A, %e %B %Y')

  .main
    .main-wrap
      .important-text
        %p <strong> #{@task.body} </strong>
      .call-to-action
        %a.call-to-action_button{href: "#{ @checkin_url }"} View it on <strong>I Done This</strong>

  .footer
    %p.centered You&rsquo;re receiving this because you're a member of #{ link_to @team.name, team_url(@team.hash_id) }. Turn it #{ link_to 'off', @email_presenter.unsubscribe_assign_task_reminders_url }.

    %p.centered
      = link_to root_url do
        = image_tag "emails/we-love-progress.png", class: 'strapline', alt: 'We love progress'
